// This is a placeholder. Replace this whole file by running:
//   dart pub global activate flutterfire_cli
//   flutterfire configure
// from the project root, after creating a Firebase project and enabling
// Authentication (Email/Password) and Firestore in the Firebase console.
// The real generated file has the same `DefaultFirebaseOptions` shape, so
// nothing else in the app needs to change once it's in place.

import 'package:firebase_core/firebase_core.dart' show FirebaseOptions;

class DefaultFirebaseOptions {
  static FirebaseOptions get currentPlatform => throw UnsupportedError(
        'firebase_options.dart não foi configurado ainda. '
        'Corre `flutterfire configure` na raiz do projeto para gerar as '
        'chaves reais do teu projeto Firebase.',
      );
}
